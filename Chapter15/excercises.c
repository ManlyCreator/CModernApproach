#include <stdio.h>

// 1.a.) Splitting a program into multiple files improves reusability and overall project organization.
//   b.) Compiling multiple source files can be tedious and error-prone at first.
// 2.) (b) - header files should only contain function prototypes
// 3.) Including a system header with quotes is valid, but a poor practice.

/*** SCRATCH WORK ***/
int main(void) {
  return 0;
}
